#include <stdio.h>

int main()
{
    int result; //result of the calculation
    result = (2+7) * 9/3;
    printf("the result is: %d\n",result); //initially there was no argument for %d hence prints the memory location
    return 0;
}